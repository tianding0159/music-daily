# 每周补库 · 运行手册（曲目 + 艺人简介，一次交付）

这是**每周那一次补库的唯一操作入口**。以前要分两次上传（曲目进 `candidates/`、
简介进 `inbox/bios/`），现在合成一份文件、一次上传。

> **给GPT的完整指令在最后一节**，直接复制粘贴即可。

---

## 一、GPT那边要做的事（顺序别乱）

1. **读状态**：`GPT_MEMORY.md` —— 曲池多少首、哪些艺人已有简介、还缺什么方向。
   自动生成、不会过期。
2. **找歌**：按 `GPT_TERRITORIES.md` 的领地与检索策略找候选，逐条按
   `GPT_CATCHUP.md` 的字段硬规则填，文案口径按 `GPT_VOICE.md`。
3. **写简介**：**凡是本批带进来的艺人，只要 `GPT_MEMORY.md` 的「已写清单」里没有，
   就必须在同一份文件里写好简介**（任务书见 `GPT_ARTIST_BIOS.md`）。
4. **自查 + 出文件**：按下面的格式和自查清单交付。

## 二、交付格式（一个文件，两个键）

```json
{
  "tracks": [ { …曲目，字段见 GPT_CATCHUP.md… } ],
  "artists": [ { "artist": "…", "bio": "…", "confidence": "high" } ]
}
```

- **`tracks`** —— 本批曲目。字段与旧格式完全一致，一条不少。
- **`artists`** —— 本批新艺人的简介。**已经在库的艺人不要重复写**（会触发覆盖告警、整批被拒）。

旧的裸数组格式（顶层直接是 `[...]`）仍然能读，但那种文件里的艺人必须**全部已在库**，
否则会被拒。所以从现在起一律用上面这个对象格式。

### 中文必须 `ensure_ascii`

```python
import json, hashlib
data = {"tracks": [...], "artists": [...]}
s = json.dumps(data, ensure_ascii=True, indent=1)   # ← 中文写成 \uXXXX
open("2026-08-10.json", "w").write(s)
print(hashlib.sha256(s.encode("ascii")).hexdigest())
```

**上一批30条简介就是没这么做而整批作废的** —— 传输通道吞掉了 `0x80–0x9F`
区间的字节（「加拿大」的 `e5 8a a0 e6 8b bf` 变成 `e5 20 e6 bf`），90% 汉字
不可复原。纯ASCII物理上不会坏。

## 三、怎么上传

GitHub网页 → 仓库 → `candidates/` 目录 → **Add file → Create new file** →
文件名 `candidates/YYYY-MM-DD.json` → 粘贴 → Commit。

**只传这一次。** `merge.yml` 会自动：

```
schema 校验 → iTunes 精确版本验真 → 去重 → 艺人简介校验 → 一起入库 → 提交
```

任一环节不过 → **整批不写盘、候选文件留在原地等修正**，`pool.json` 和
`artists.json` 一个字都不会变。

也可以本地先体检（不写盘、能验出「新艺人缺简介」）：

```bash
python3 scripts/merge_candidates.py --dry-run
```

## 四、会被拒的硬条件

| 拒收原因 | 说明 |
|---|---|
| `mood_tags` 不在32词受控表内 | 必须照抄，写近义词会被拒（`scripts/mood_vocab.py`）|
| `why` 超过2句 | 严格上限 |
| iTunes查不到 / 版本对不上 / 艺人不符 | 宁缺不错，自动隔离 |
| **新艺人没带简介** | 报错会**逐个点名**缺哪位 |
| 简介含黑名单词 / 「让人」「令人」 | 判据与专用通道完全一致 |
| 简介 `confidence` 非 `high`/`low` | 只这两个值 |
| 简介里英文地名没译成中文 | 人名 / 厂牌 / 专辑名 / 乐队名保留英文是对的 |
| 简介里汉字之间夹空格 | 换词时原英文两侧的空格要一起删 |
| 简介覆盖已有艺人且内容不同 | 已在库的别重复写 |

## 五、交付时附上这些自查数字

让质量漂移可见 —— 单看每批都合规、连起来才看得出退步（batch05就这么滑过去过）。

- 曲目数 · 新艺人数 · 简介数（**新艺人数应等于简介数**）
- 流派 / 气质 / 年代分布
- 简介长度范围 · high/low分布
- 黑名单词命中数（应为0）
- 「让人 / 令人」出现次数（应为0）
- 未翻译英文地名条数（应为0）
- 与「已写清单」的重叠数（应为0）

## 六、其它文档

| 文档 | 什么时候看 |
|---|---|
| [`GPT_MEMORY.md`](GPT_MEMORY.md) | **每次开工先读** —— 当前状态、已写清单、缺口 |
| [`GPT_TERRITORIES.md`](GPT_TERRITORIES.md) | 20个领地 + 四层检索策略 + 实测命中率 |
| [`GPT_CATCHUP.md`](GPT_CATCHUP.md) | 曲目字段硬规则（受控词表、版本错配）|
| [`GPT_VOICE.md`](GPT_VOICE.md) | 用语文风（黑名单47词、句式配额、站内固定用语）|
| [`GPT_ARTIST_BIOS.md`](GPT_ARTIST_BIOS.md) | 简介任务书（正反例、四层结构）|
| [`docs/profile.md`](docs/profile.md) | 口味画像（选曲唯一依据）|

---

## 七、完整流程（定时补库，端到端）

```
  ┌ GPT 那边（定时任务，无人值守）
  │  ① 读 GPT_MEMORY.md   → 当前池 / 已写清单 / 缺口方向
  │  ② 按 TERRITORIES 找歌 → 按 CATCHUP 填字段 → 按 VOICE 写文案
  │  ③ 给本批【新艺人】写简介（已在库的跳过）
  │  ④ 自查 → 输出一个 ensure_ascii 的 JSON + SHA-256
  └─────────────────────────────────────────────
  ┌ 你（一步）
  │  ⑤ GitHub 网页 → candidates/ → Add file → 粘贴 → Commit
  └─────────────────────────────────────────────
  ┌ 仓库（全自动，merge.yml）
  │  ⑥ schema 校验 → iTunes 精确验真 → 去重
  │  ⑦ 艺人简介校验（判据与 inbox/bios 专用通道完全一致）
  │  ⑧ 曲目 + 简介一起入库 → 提交
  │  ⑨ 微信推结果
  └─────────────────────────────────────────────
       ↓ 被拒时
  ⑩ 微信告诉你原因 → 把原因转给 GPT → GPT 修 → 回到 ⑤
     （候选文件原地保留，pool.json / artists.json 一个字没动）
```

**微信会收到四种结果**，只有两种需要你动手：

| 通知 | 你要做什么 |
|---|---|
| `补库完成：+N 首` | 不用管 |
| `补库遇到网络问题` | **不用管**，下次自动重试 |
| `补库未通过校验` | 把 Actions 日志里的拒收条目转给 GPT，修好重传 |
| `补库被拒：新艺人缺简介` | 同上，日志里点名了缺哪位 |

## 八、给 GPT 定时任务的指令（复制这一整段）

> 这段是给**无人值守的定时任务**用的：中途没人能答疑，所以每个决策都写死了默认值。

```text
你是「硅基音乐日报」的补库编辑。这是每周一次的定时任务，中途没有人能回答你，
所有拿不准的地方按下面的默认规则处理，不要停下来问。

【第一步：读状态】
从这个仓库根目录读（每次都要重读，内容会变）：
  GPT_WEEKLY.md      ← 本任务的完整流程与交付格式，一切以它为准
  GPT_MEMORY.md      ← 当前状态：曲池多少、哪些艺人已有简介、还缺什么方向
  GPT_TERRITORIES.md ← 去哪找、怎么找
  GPT_CATCHUP.md     ← 曲目每个字段的硬规则
  GPT_VOICE.md       ← 文案用语口径（黑名单词、句式配额）
  GPT_ARTIST_BIOS.md ← 艺人简介怎么写
  docs/profile.md    ← 口味画像，选曲的唯一依据

【第二步：找歌】
目标 50 首。严格照 profile 选，不按流派凑数；黑名单流派与制作特征命中即排除；
必须有可哼唱的旋律。优先补 GPT_MEMORY.md 里标出的缺口方向。
不要选已在池里的曲目（已写清单和池子在 GPT_MEMORY.md 里）。

【第三步：写简介】
本批带进来的艺人，只要 GPT_MEMORY.md 的「已写清单」里没有，就必须写简介。
已经在库的艺人不要重复写（会触发覆盖告警、整批被拒）。
简介要给可核实的事实：本名、生年、出生/常驻地、厂牌、谁签的、代表专辑、
用什么设备、之前在哪个乐队。不要写成 artist_oneliner 的扩写。

【第四步：出文件】
一个文件，顶层对象，两个键：
  {"tracks": [ …曲目… ], "artists": [ {"artist":"…","bio":"…","confidence":"high"} ] }
中文必须 ensure_ascii=True 写成 \uXXXX（纯 ASCII），并附上文件的 SHA-256。
这一步不能省 —— 曾有一批因为编码在传输中损坏而整批作废。

【第五步：自查并附上数字】
曲目数、新艺人数、简介数（**新艺人数必须等于简介数**）、流派/气质/年代分布、
简介长度范围、黑名单词命中数（0）、「让人/令人」次数（0）、
未翻译英文地名条数（0）、与已写清单的重叠数（0）。

━━━ 无人值守的默认规则（照做，不要问）━━━

· 找不满 50 首 → **交实际找到的数量，不要用平庸的曲子凑数**。
  质量优先于数量，这条永远成立。宁可这周只交 30 首。

· 某位艺人查不到可核实的事实 → **连他的曲目一起从这批里拿掉**。
  因为规则是「新艺人必须有简介」，写不出扎实简介就不要带这首歌进来，
  下次有材料了再说。绝对不要编造生年、厂牌、专辑名来凑一段简介。

· 拿不准某个事实 → 把 confidence 写成 "low"，或者干脆不写那一句。
  **不要编。** 编造的事实会直接显示在网站上。

· mood_tags 拿不准 → 只从 32 个受控英文词里挑最接近的，不要造新词。
  写近义词会被整批拒收。

· 地名拿不准中文译名 → 用通行译名（Brooklyn→布鲁克林、Nashville→纳什维尔）。
  人名、厂牌名、专辑名、乐队名保留英文是对的，不要翻。
  注意：《Michigan》是专辑名、Oregon 可能是乐队名 —— 看语境，别一律当地名。

· 汉字之间不要有空格。中文与英文之间的空格是对的，保留。

· 文档之间有冲突 → 以 GPT_WEEKLY.md 为准。

━━━ 被拒了怎么办 ━━━

用户会把拒收原因转给你。原封不动修那几条，其余不要动，重新出整个文件
（含新的 SHA-256）。不要因为一条被拒就重写整批 —— 那会把本来对的也改坏。
```
